[env]
# nginx-sys v0.2.1 (bundled with ngx 0.4.1) hardcodes zlib 1.3, which has
# been removed from zlib.net. Override to 1.3.2 which is available.
# Remove this once ngx-rust is upgraded to v0.5.0+.
ZLIB_VERSION = "1.3.2"
